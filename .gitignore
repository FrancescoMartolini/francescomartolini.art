# Generate automaticamente a ogni deploy da genera-route-statiche.py
# (pagine 200 invece di 404) e da genera-feed-rss.py (feedrss.xml) —
# non vanno committate, si ricreano da sole (vedi README).
/progetti/
/chi-sono/
/fotografie-commerciali/
/intervalli/
/taccuino/
/istanze/
/playlist/
feed.xml

node_modules/
__pycache__/
*.pyc
TEMPLATE/

# Generati a ogni deploy (minificazione one-shot via terser/clean-css,
# vedi README sezione "Script da inserire nel builder di Cloudflare") —
# non vanno committati, si ricreano da soli. Il sorgente leggibile
# resta js/libro.js e css/stile.css: si continua a editare quelli.
js/libro.min.js
css/stile.min.css

.DS_Store
